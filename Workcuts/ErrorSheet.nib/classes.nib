{
    IBClasses =     (
        {
    CLASS = FirstResponder;
    LANGUAGE = ObjC;
    SUPERCLASS = NSObject;
},
        {
    ACTIONS =     {
        activate = id;
        clearRecents = id;
        close = id;
        closeErrorSheet = id;
        editWorkcuts = id;
        open = id;
        openRecent = id;
        openShortcut = id;
        openShortcutFromDock = id;
        openShortcutFromStatusMenu = id;
        popupMenu = id;
    };
    CLASS = WorkcutsAppDelegate;
    LANGUAGE = ObjC;
    OUTLETS =     {
        activateMenuItem = NSMenuItem;
        clearRecentsMenuItem = NSMenuItem;
        errorMessage = NSTextField;
        errorView = NSTextView;
        evalErrorSheet = NSWindow;
        recentProjectsMenu = NSMenu;
        shortcutsMenu = NSMenu;
        statusMenu = NSMenu;
    };
    SUPERCLASS = NSObject;
}
    );
    IBVersion = 1;
}